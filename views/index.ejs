<% if (blogs.length) { %>
    <% for( let blog = 0; blog < blogs.length; blog++ ) { %>
    <h1><%= blogs[blog].title %></h1>
    <p><%= blogs[blog].intro %></p>
    <% } %>
    
    <!-- <% blogs.forEach(blog => { %>
        <h1><%= blog.title  %></h1>
        <p><%= blog.intro  %></p>
        <% }); %> -->
<% } else { %>
    <h1>No blogs to show</h1>
 <% } %>